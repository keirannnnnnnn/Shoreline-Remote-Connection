<Window x:Class="VNCClient.Views.VNCViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VNCClient"
        Title="{Binding WindowTitle}" 
        Height="700" Width="1000"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceBrush}"
        FontFamily="Segoe UI">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar -->
        <Border Grid.Row="0" 
                Background="White" 
                BorderThickness="0,0,0,1" 
                BorderBrush="{StaticResource BorderBrush}"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Connection info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🖥️" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Connection.DisplayName}" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Connection.ConnectionString}" 
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="8,0,0,0"/>
                </StackPanel>
                
                <!-- Status indicator -->
                <StackPanel Grid.Column="1" 
                           Orientation="Horizontal" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" 
                             Fill="{Binding StatusColor}" 
                             Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding StatusText}" 
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
                
                <!-- Action buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Connect" 
                            Style="{StaticResource ModernButtonStyle}"
                            Command="{Binding ConnectCommand}"
                            Visibility="{Binding IsConnected, Converter={x:Static local:BooleanToVisibilityConverter.Inverted}}"
                            Margin="0,0,8,0"/>
                    <Button Content="Disconnect" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding DisconnectCommand}"
                            Visibility="{Binding IsConnected, Converter={x:Static local:BooleanToVisibilityConverter.Default}}"
                            Margin="0,0,8,0"/>
                    <Button Content="⚙" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            ToolTip="Settings"
                            Width="32"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- VNC Display Area -->
        <Border Grid.Row="1" 
                Background="Black" 
                Margin="1">
            <Grid>
                <!-- VNC Control placeholder -->
                <Border Name="VncDisplayBorder" 
                        Background="Black">
                    <!-- This is where the VNC control would be embedded -->
                </Border>
                
                <!-- Connection overlay -->
                <Border Background="{StaticResource CardBrush}"
                        Visibility="{Binding IsConnected, Converter={x:Static local:BooleanToVisibilityConverter.Inverted}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Style="{StaticResource ModernCardStyle}"
                        Padding="32">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔗" 
                                   FontSize="48" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="Click Connect to start VNC session" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding Connection.ConnectionString}" 
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,16"/>
                        <Button Content="Connect Now" 
                                Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding ConnectCommand}"/>
                    </StackPanel>
                </Border>
                
                <!-- Loading overlay -->
                <Border Background="#80000000"
                        Visibility="{Binding IsConnecting, Converter={x:Static local:BooleanToVisibilityConverter.Default}}">
                    <StackPanel HorizontalAlignment="Center" 
                               VerticalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" 
                                    Width="200" 
                                    Height="4"
                                    Background="{StaticResource BorderBrush}"
                                    Foreground="{StaticResource PrimaryBrush}"/>
                        <TextBlock Text="Connecting..." 
                                   FontSize="16"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,16,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="{StaticResource CardBrush}" 
                BorderThickness="0,1,0,0" 
                BorderBrush="{StaticResource BorderBrush}"
                Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="{Binding StatusMessage}" 
                           FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" 
                           Orientation="Horizontal">
                    <TextBlock Text="Quality: High" 
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0"/>
                    <TextBlock Text="{Binding ConnectionDuration}" 
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>